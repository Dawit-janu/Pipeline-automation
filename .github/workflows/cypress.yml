name: Cypress Automation Test

on:
  # 🚀 Push ke main → trigger (merge dari QA branch setelah fix)
  push:
    branches: [ main ]

  # 🔀 Pull Request ke main → trigger saat QA buat PR dari branch fix
  pull_request:
    branches: [ main ]

  # 🖐 Manual trigger
  workflow_dispatch:

  # ✅ STEP 2 — SCHEDULER (DITAMBAHKAN DI SINI)
  schedule:
   #- cron: '0 1 * * *'   # 08:00 WIB (UTC+7)
   #- cron: '40 20 * * *'
     #- cron: '0 18 * * 0-4'
      - cron: '*/5 * * * *'


jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Detect trigger & start time
        run: |
          echo "TRIGGER=${{ github.event_name }}" >> $GITHUB_ENV
          echo "JOB_START_TIME=$(date +%s)"       >> $GITHUB_ENV

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y jq bc

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install project dependencies
        run: npm ci

      # ============================================================
      # RUN CYPRESS
      # ============================================================
      - name: Run Cypress Tests
        continue-on-error: true
        run: |
          npx cypress run \
            --record \
            --key ${{ secrets.CYPRESS_RECORD_KEY }} \
            2>&1 | tee cypress-raw.txt

      - name: Strip ANSI color codes
        if: always()
        run: |
          sed 's/\x1B\[[0-9;]*[mKHF]//g' cypress-raw.txt > cypress-output.txt

      # ============================================================
      # ALLURE
      # ============================================================
      - name: Generate Allure Report
        if: always()
        run: |
          npm install -g allure-commandline --quiet
          if [ -d allure-results ] && [ "$(ls -A allure-results 2>/dev/null)" ]; then
            allure generate allure-results --clean -o allure-report
          fi

      - name: Upload Allure Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-${{ github.run_number }}
          path: allure-report/
          retention-days: 30

      # ============================================================
      # EXTRACT STATS
      # ============================================================
      - name: Extract test summary
        if: always()
        run: |
          # Duration
          JOB_END_TIME=$(date +%s)
          DURATION_SEC=$(( JOB_END_TIME - JOB_START_TIME ))
          MINUTES=$((DURATION_SEC / 60))
          SECONDS=$((DURATION_SEC % 60))

          # Stats dari cypress terminal output
          PASSING=$(grep -E '^\s+[0-9]+ passing' cypress-output.txt | tail -1 | grep -oE '[0-9]+' | head -1)
          FAILING=$(grep -E '^\s+[0-9]+ failing' cypress-output.txt | tail -1 | grep -oE '[0-9]+' | head -1)
          PENDING=$(grep -E '^\s+[0-9]+ pending' cypress-output.txt | tail -1 | grep -oE '[0-9]+' | head -1)

          PASSING=${PASSING:-0}
          FAILING=${FAILING:-0}
          PENDING=${PENDING:-0}
          TOTAL=$(( PASSING + FAILING + PENDING ))

          if [ "$TOTAL" -gt 0 ]; then
            PASS_RATE=$(echo "scale=1; $PASSING * 100 / $TOTAL" | bc)
          else
            PASS_RATE="0"
          fi

          # Nama spec files yang dijalankan (untuk suite name)
          SUITE_LIST=$(grep -oE 'Running:\s+\S+' cypress-output.txt \
            | sed 's/Running:\s*//' \
            | tr '\n' ', ' \
            | sed 's/, $//')

          echo "TOTAL_TESTS=$TOTAL"                    >> $GITHUB_ENV
          echo "PASSED_TESTS=$PASSING"                 >> $GITHUB_ENV
          echo "FAILED_TESTS=$FAILING"                 >> $GITHUB_ENV
          echo "PENDING_TESTS=$PENDING"                >> $GITHUB_ENV
          echo "TEST_DURATION=${MINUTES}m ${SECONDS}s" >> $GITHUB_ENV
          echo "PASS_RATE=${PASS_RATE}%"               >> $GITHUB_ENV
          echo "SUITE_LIST=$SUITE_LIST"                >> $GITHUB_ENV

          echo "Total=$TOTAL | Passed=$PASSING | Failed=$FAILING | Duration=${MINUTES}m ${SECONDS}s"

      - name: Extract failed test names
        if: always()
        run: |
          # Parse failed test names dari section "failing" di output Cypress
          # Format Cypress:
          #   1) Suite Title
          #        test title:
          FAILED_LIST=$(awk '
            /^[[:space:]]+[0-9]+\)/ {
              capture=1
              suite=""
              next
            }
            capture==1 && /^[[:space:]]+[^[:space:]]/ {
              line=$0
              gsub(/^[[:space:]]+/, "", line)
              gsub(/:$/, "", line)
              print "• " line
              capture=0
            }
            /^[[:space:]]*$/ { capture=0 }
          ' cypress-output.txt | sort -u | head -20)

          echo "=== FAILED LIST DEBUG ==="
          echo "$FAILED_LIST"

          echo "FAILED_TEST_DETAILS<<EOF" >> $GITHUB_ENV
          echo "$FAILED_LIST"             >> $GITHUB_ENV
          echo "EOF"                      >> $GITHUB_ENV

      # ============================================================
      # DISCORD NOTIFICATION — format mirip Katalon
      # ============================================================
      - name: Send Discord Notification
        if: always()
        run: |
          EXEC_TIME=$(TZ='Asia/Jakarta' date "+%Y-%m-%d %I:%M:%S %p")
          DURATION_LABEL="${TEST_DURATION}"

          TOTAL=${TOTAL_TESTS:-0}
          PASSED=${PASSED_TESTS:-0}
          FAILED=${FAILED_TESTS:-0}

          if [ "$FAILED" != "0" ]; then
            STATUS_TEXT="has failed"
            RESULT_ICON="❌"
            COLOR=15158332
            RESULT_LABEL="$FAILED test results failed"
          else
            STATUS_TEXT="has passed"
            RESULT_ICON="✅"
            COLOR=3066993
            RESULT_LABEL="$TOTAL test results passed"
          fi

          # Trigger label
          case "$TRIGGER" in
            schedule)          TRIGGER_LABEL="Scheduled"    ;;
            push)              TRIGGER_LABEL="Push to main" ;;
            pull_request)      TRIGGER_LABEL="Pull Request" ;;
            workflow_dispatch) TRIGGER_LABEL="Manual"       ;;
            *)                 TRIGGER_LABEL="$TRIGGER"     ;;
          esac

          # Branch / PR info
          if [ "$TRIGGER" = "pull_request" ]; then
            BRANCH_INFO="PR #${{ github.event.pull_request.number }} (${{ github.head_ref }})"
          else
            BRANCH_INFO="${{ github.ref_name }}"
          fi

          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          CLOUD_URL="https://cloud.cypress.io/projects/wipkva/runs"
          ARTIFACT_URL="${RUN_URL}#artifacts"

          # Suite name — pakai spec list atau fallback ke branch
          if [ -n "$SUITE_LIST" ]; then
            SUITE_NAME="$SUITE_LIST"
          else
            SUITE_NAME="${{ github.ref_name }}"
          fi

          # Failed test list sebagai string
          if [ "$FAILED" != "0" ] && [ -n "$FAILED_TEST_DETAILS" ]; then
            FAILED_BLOCK="$FAILED test results failed\n$FAILED_TEST_DETAILS"
          elif [ "$FAILED" != "0" ]; then
            FAILED_BLOCK="$FAILED test results failed\n• (lihat Cypress Cloud untuk detail)"
          else
            FAILED_BLOCK=""
          fi

          # Build description mirip Katalon
          REPO="${{ github.repository }}"
          DESC="Test Run [#${{ github.run_number }}]($RUN_URL) of the **${REPO##*/}** ${STATUS_TEXT}."

          BODY="$(printf '%s\n\n📦 %s\n🕐 %s (%s)\n%s %s test results' \
            "$DESC" \
            "$SUITE_NAME" \
            "$EXEC_TIME" \
            "$DURATION_LABEL" \
            "$RESULT_ICON" \
            "$TOTAL")"

          if [ -n "$FAILED_BLOCK" ]; then
            BODY="$(printf '%s\n\n%b' "$BODY" "$FAILED_BLOCK")"
          fi

          FOOTER="${REPO} | ${BRANCH_INFO} | ${TRIGGER_LABEL}"

          jq -n \
            --arg body   "$BODY" \
            --arg footer "$FOOTER" \
            --arg cloudUrl "$CLOUD_URL" \
            --arg artifactUrl "$ARTIFACT_URL" \
            --argjson color $COLOR \
          '{
            embeds: [{
              description: $body,
              color: $color,
              fields: [
                { name: "☁️ Cypress Cloud", value: ("[Open Dashboard →](" + $cloudUrl + ")"),      inline: true },
                { name: "📦 Allure Report", value: ("[Download Artifact →](" + $artifactUrl + ")"), inline: true }
              ],
              footer: { text: $footer },
              timestamp: (now | todateiso8601)
            }]
          }' \
          | curl -s -H "Content-Type: application/json" \
              -X POST \
              -d @- \
              ${{ secrets.DISCORD_WEBHOOK }}